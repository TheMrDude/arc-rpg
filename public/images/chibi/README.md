# Chibi Character Images

Drop chibi PNG files here named 1.png through 47.png. The NPC library will automatically display them.

When images are present, update `ChibiAvatar` in `app/campaign/dm/page.js` to render `<img src={`/images/chibi/${id}.png`} />` instead of the colored circle placeholder.
